# ClauseGPT - Intelligent Policy Query System

## Quickstart (local, free)
1. Create a Python venv:
   ```bash
   python -m venv venv
   source venv/bin/activate   # Windows: venv\Scripts\activate
   pip install -r requirements.txt
